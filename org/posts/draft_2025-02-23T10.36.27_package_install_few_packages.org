#+title: Real-Life Emacs Problem Solving
#+author: Luis Henriquez-Perez

This is the original function that I first thought worked.  Only problem is I realized that [[file:/home/luis/Documents/MyBlog/org/documentation/29.4-helpful-callable--package--query-desc.html][package--query-desc]] only works for installed packages.  I want this package link function to be able to insert the link of any package.

#+begin_src emacs-lisp
(defun! oo-expand-org-package-link ()
  "Prompt for package and insert the corresponding org package link."
  (set! desc (package--query-desc))
  (set! link (cdr (assoc :url (and desc (package-desc-extras desc)))))
  (insert (format "[[%s][%s]]" link (package-desc-name desc))))
#+end_src

For whatever reason [[file:/home/luis/Documents/MyBlog/org/documentation/29.4-helpful-callable--package-install.html][package-install]] does the right thing so I just copy its code
into the function.  I look again at [[file:/home/luis/Documents/MyBlog/org/documentation/29.4-helpful-callable--package-browse-url.html][package-browse-url]] but I realize it only has.

#+begin_src emacs-lisp
(defun! oo-expand-org-package-link ()
  "Prompt for package and insert the corresponding org package link."
  (set! link (intern (completing-read
                      "Install package: "
                      (mapcan
                       (lambda (elt)
                         (and (or (and (or current-prefix-arg
                                           package-install-upgrade-built-in)
                                       (package--active-built-in-p (car elt)))
                                  (not (package-installed-p (car elt))))
                              (list (symbol-name (car elt)))))
                       package-archive-contents))))
  (set! name link)
  (set! link "foo")
  (insert (format "[[%s][%s]]" link name)))

(defun! oo-expand-package-link ()
  "Prompt for package and insert the corresponding package link."
  (insert "foo"))
#+end_src

