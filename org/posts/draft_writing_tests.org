It was much easier testring when I used [[][eldev]].  It was much easier using
eldev when I did not use the built-in Emacs package manager, package.el because
it did not conflict with Emacs.  Eldev is designed to be used for developing
Emacs packages so using it for your Emacs configuration is an unorthodox usage,
and it works by pretending that your Emacs config is a providing a package
called "init".  Eldev fairly assumes that no package is going to be configuring
package.el and installing packages.  But for your configuration this is not the case.  In the examples where Boris has used it, it has been with
[[][straight.el]] or, more recently, the [[][elpaca]] package manager so there
was no conflict.  So now I have to figure out what to do.

I was very much influenced by Boris's Emacs configuration when writing my own in
terms of.
