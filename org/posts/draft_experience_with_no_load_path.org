#+title:draft experience with no load path
#+author:Luis Henriquez-Perez

Today I experienced a weird problem in my Emacs configuration.  Basically when I tried to restart it it could not find any of my packages in the load-path.  I scowered my configuration trying to find hat was the problem, but I barely made any changes to [[][init-packag.el]].  I just added [[][try]] as a new Emacs package.  Finally it occurred to me to try calling [[][]].  And voilà.  I had forgotten that when you use =package-quickstart= you must ensure you.

Obviously I want to automate this so I do not need to remember to call it every time.  I would like to just automatically call it after installing a package but I think it is expensive so it would be better to do it during idle time.

#+begin_src emacs-lisp
#+end_src

