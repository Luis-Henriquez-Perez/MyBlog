I was very confused about byte-compilation. 

Compiling my Emacs configuration.  At first I was using [[][eldev]] but when I
began using =package.el= I would run into problems with byte-compilation when
loading my [[][init-package]] file.  Basically =eldev= uses package.el to install
its own dependencies and it assumes that a package would not use package.el
(which is a fair assumption as it would be crazy for a package to install other
packages).  The compiling and the testing still works well.  One of the things I
realized is that you need to load your init file if you compile externally.  The
reason for this is that the batch Emacs does not know where any of the files in
the load-path are.  One question is if I could give it the load-path would it
work.  I am actually not sure.
